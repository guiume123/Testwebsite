{
  "name": "baus-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"php -S localhost:8000\" \"vite\"",
    "dev:vite": "vite",
    "dev:full": "concurrently \"php -S localhost:8000\" \"vite\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  }
}
